import { RecipeDirective } from './recipe.directive';

describe('RecipeDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeDirective();
    expect(directive).toBeTruthy();
  });
});
