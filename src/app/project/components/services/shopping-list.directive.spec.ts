import { ShoppingListDirective } from './shopping-list.directive';

describe('ShoppingListDirective', () => {
  it('should create an instance', () => {
    const directive = new ShoppingListDirective();
    expect(directive).toBeTruthy();
  });
});
